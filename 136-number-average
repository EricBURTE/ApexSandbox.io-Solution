public Decimal average(List<Decimal> numbers){
    Decimal dTotal = 0, dAverage = 0;
    Integer iCompteur = 0;

    for (Decimal dCurrent : numbers) {
        dTotal += dCurrent;
        iCompteur++;
    }

    dAverage = dTotal / iCompteur;
    return (dAverage.setScale(2));
}