public String formatName(String firstName, String lastName) {
    String sName = '';
    if (!String.IsBlank(firstName)) {
        sName += lastName;
    }
    if (!String.IsBlank(firstName) && !String.IsBlank(firstname)){
        sName += ', ';
    }
    if (!String.IsBlank(firstName)) {
        sName += firstName;
    }
    return sName;
}