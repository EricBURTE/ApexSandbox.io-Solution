public Boolean duplicateContacts(Contact c1, Contact c2) {
    boolean bDuplicate = false;
    if (   (c1.Phone == c2.Phone && c1.Phone != null & c2.Phone != null)
        || (c1.Email == c2.Email && c1.Email != null & c2.Email != null) ) {
        bDuplicate = true;
    }
    return bDuplicate;
}