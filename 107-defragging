public void defragging(Integer[] memory) {
    Integer index = 0; 
    while (index < memory.size()) {
        if(memory[index] == -1) {
            memory.add(memory.remove(index));
        }
        else { index++ ;}
    }
}