public String divide(Integer a, Integer b){
    String sResult;
    try {
        Integer iEntireDivisor = Integer.valueOf(a/b);
        sResult = String.valueOf(iEntireDivisor);
    }
    catch (Exception e){
        return e.getMessage();
    }
    return sResult;
}