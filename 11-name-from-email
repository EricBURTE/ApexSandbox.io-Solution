public String nameFromEmail(String email) {
    String sFirstname = email.substring(1,email.indexOf('.')).capitalize();
    String sLastname = email.substring(email.indexOf('.')+1,email.indexOf('@')).capitalize();
    return sFirstname + ' ' + sLastname;
}